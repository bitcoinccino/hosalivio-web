# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# Intentionally empty. The default generated stubs (`one: {}` / `two: {}`)
# insert a User with a null full_name, which violates the NOT NULL constraint
# and breaks fixture loading for the whole suite. Add real fixtures here (with
# full_name + agency) when a test needs persisted users.
